# Building Energy Pipeline v8.0 — Python dependencies
# Tested with Python 3.10+ (Windows 10/11, CPU-only)

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
matplotlib>=3.7
openpyxl>=3.1          # reads the RECS codebook workbook (xlsx)
optuna>=3.4            # hyperparameter optimisation (enabled with --optimize)
shap>=0.43             # feature attribution (optional; gain-based fallback if absent)

# Optional — third stacking base learner (auto-detected when installed):
# catboost>=1.2
